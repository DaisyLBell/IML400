function setup(){
   createCanvas(windowWidth, windowHeight);
    background(0);
}

function draw(){
    fill(255, 255, 255, 5);
    ellipse(mouseX, mouseY, 60, 60);
    
}

function mouseClicked() {
        background (0);
    }